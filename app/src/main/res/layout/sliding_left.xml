<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="200dp"
    android:layout_height="match_parent"
    android:background="@drawable/btn_small_white_pressed"
    android:orientation="vertical">

    <ListView
        android:id="@+id/listview_left"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"></ListView>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:layout_marginLeft="10dp"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/tv_setting"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_vertical"
            android:layout_weight="1"
            android:clickable="true"
            android:drawableLeft="@drawable/selector_setting"
            android:drawablePadding="5dp"
            android:gravity="center_vertical"
            android:text="设置"
            android:textColor="@drawable/selector_text_setting"
            android:textSize="16sp" />

        <TextView
            android:id="@+id/tv_exit"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_vertical"
            android:layout_weight="1"
            android:clickable="true"
            android:drawableLeft="@drawable/selector_exit"
            android:drawablePadding="10dp"
            android:gravity="center_vertical"
            android:text="退出"
            android:textColor="@drawable/selector_text_setting"
            android:textSize="16sp" />

    </LinearLayout>

</LinearLayout>
